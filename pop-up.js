document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});


document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up1");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up2");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up3");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up4");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up5");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up6");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up7");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up8");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up9");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up10");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up11");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up12");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up13");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up14");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up15");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up16");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up17");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});

document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".pop-up18");

    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp); // หยุดการตรวจสอบหลังจากแสดงครั้งแรก
            }
        });
    });

    observer.observe(popUp);
});


document.addEventListener("DOMContentLoaded", () => {
    const popUp = document.querySelector(".popt");
    const text = popUp.textContent;
    popUp.textContent = ""; // ล้างข้อความเดิมก่อน
    popUp.style.opacity = "1"; 
    popUp.style.visibility = "visible";

    // แบ่งข้อความออกเป็นตัวอักษร และเพิ่ม `<span>` รอบแต่ละตัว
    text.split("").forEach((char, index) => {
        const span = document.createElement("span");
        span.textContent = char;
        span.style.animationDelay = `${index * 0.1}s`; // ให้ตัวอักษรปรากฏทีละตัว
        popUp.appendChild(span);
    });

    // ใช้ IntersectionObserver ให้ข้อความเริ่มแสดงเมื่อเข้าสู่หน้าจอ
    const observer = new IntersectionObserver(entries => {
        entries.forEach(entry => {
            if (entry.isIntersecting) {
                popUp.classList.add("show");
                observer.unobserve(popUp);
            }
        });
    });

    observer.observe(popUp);
});